require 'spec_helper'

describe Arscraper do
  it 'has a version number' do
    expect(Arscraper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
